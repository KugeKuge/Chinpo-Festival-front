export const imageDisplaySize = { width: 500, height: 500 };
export const styles = {
	imageContainer: {
		width: imageDisplaySize.width,
		height: imageDisplaySize.height,
		marginTop: 10,
        display: 'flex',
		justifyContent: 'center',
		alignItems: 'center',
	}
};